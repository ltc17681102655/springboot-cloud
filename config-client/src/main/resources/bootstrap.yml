server:
  port: 8084

spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      uri: http://localhost:8083/
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/


