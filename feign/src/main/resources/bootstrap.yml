server:
  port: 8085

spring:
  application:
    name: feign
  cloud:
    config:
      label: master
      uri: http://localhost:8083/
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/